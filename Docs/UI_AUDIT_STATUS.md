# UI_AUDIT_STATUS — log de auditoria de tela (ui-reviewer)

Memória entre execuções do subagent `ui-reviewer` (`.claude/agents/ui-reviewer.md`). Uma linha por
tela por execução. Lista mestra das telas:
`Docs/stitch_app_visual_identity/PENDENCIAS_TELAS.md` (17 telas).

**Como usar:** antes de um modo varredura, priorize telas sem entrada aqui ou com a entrada mais
antiga. Depois de auditar, acrescente a linha correspondente.

Cada linha traz, além dos defeitos encontrados, a avaliação **"o que seria ideal"** — o julgamento
sobre se a tela poderia ser mais polida/clara mesmo sem bug evidente (pedido explícito do usuário em
25/08/2026, para essa avaliação não se perder entre rodadas).

| Data | Tela | Alvo | Status | Achados / ação |
|---|---|---|---|---|
| 2026-08-24 | A. Home — Mapa de Aprendizado | `apps/web/src/app/(shell)/page.tsx` | Auditada — corrigida | Título da unidade duplicado (h3 + card), badge/borda "em andamento" em laranja violando a One Job Per Color Rule, barra da Meta Diária espremida a ~90px em viewport de 390px. Todos corrigidos. Passe funcional ao vivo: os 6 elementos interativos navegam de verdade. Detector do impeccable: 0 achados. |
| 2026-08-24 | A. Home — Mapa de Aprendizado | `apps/mobile/src/app/(tabs)/index.tsx` | Auditada por código — **verificação visual pendente** | Mesmo título duplicado (corrigido), nó "concluído" mostrando o ícone da matéria em vez do check (corrigido, paridade com web + Stitch), Meta Diária em `flexDirection: row` espremendo barra+rótulo+botão numa largura de telefone (corrigido). Screenshot da Home real **não** foi possível: mobile só tem sessão real via Supabase Auth, sem modo demonstração — ver pendência #1 abaixo. |

| 2026-08-25 | B. Sessão de Lição — Quiz | `apps/web/.../[lessonId]/sessao/page.tsx` + `components/features/quiz/` | Auditada — corrigida | **Bug medido:** em 390px a barra de progresso da lição ficava com **10px de largura** — os 5 corações + gemas + download + sair comiam a linha inteira e o dado mais importante do cabeçalho virava um pontinho cinza. Corrigido com `flex-wrap` (a barra cai pra linha própria abaixo de `sm`); remedido depois: **358px**. **Ideal:** no desktop a pergunta ficava colada no topo com ~450px de vazio até a barra de ação — adicionado `md:justify-center` (telefone segue no topo, onde a rolagem resolve). **Divergências do Stitch julgadas e mantidas:** 5 corações individuais em vez de coração+numeral (o `arqlearn_stitch_ui_spec.md` manda 5 ícones, e o comentário no `HeartsRow` já registra isso — a referência de tela é que está desalinhada da spec escrita); ausência dos ícones ilustrativos por opção (não há dado no backend pra escolhê-los). **Pendente:** botão "Verificar" desabilitado fica em ~1.9:1 de contraste (`disabled:opacity-50` do `Button`) — não corrigido aqui porque mexer no `Button` reestiliza todo botão desabilitado do app sem re-auditoria. |
| 2026-08-25 | B. Sessão de Lição — Quiz | `apps/mobile/.../[lessonId]/sessao.tsx` + `components/features/quiz/` | Auditada por código — **verificação visual pendente** | Mesmo esmagamento da barra de progresso (pior: no telefone é sempre estreito) — corrigido com a barra em linha própria. **Drift de paridade achado de quebra:** o `StatPill` das gemas estava em `tone="primary"` (azul) só aqui — único ponto do app pintando gema de azul, contra `secondary` no `TopAppBar` do próprio mobile e no `QuizHeader` do web; alinhado. Screenshot impossível — ver pendência #1. |
| 2026-08-25 | C. Resumo da Lição | `apps/web/.../[lessonId]/resumo/page.tsx` + `components/features/lessonSummary/` | Auditada — corrigida, **1 pergunta aberta** | **Fidelidade:** faltava o selo de conclusão (círculo verde com check) que a referência do Stitch tem no topo — era o único sinal não-textual de "deu certo" e não existia; adicionado. **Elemento decorativo (pergunta #3 abaixo):** `moduleProgressPercent={75}` é literal — a barra "Progresso do módulo" mostra 75% sempre. **Divergências do Stitch julgadas e mantidas:** botão "Continuar para o Mapa" em azul e não laranja (o `DESIGN.md` do próprio projeto manda azul = navegação, laranja = gamificação — a regra escrita vence a tela); valor acima do rótulo no `StatCard` (Stitch inverte) por escaneabilidade; 4º card "Vidas Restantes" em vez de "Gemas" (gemas aparecem quando a lição rende gemas de verdade). **Ideal:** fora isso, a tela já está no ponto — hierarquia, ritmo e densidade batem com a referência. |
| 2026-08-25 | C. Resumo da Lição | `apps/mobile/.../[lessonId]/resumo.tsx` + `components/features/lessonSummary/` | Auditada por código — **verificação visual pendente** | Mesmo selo de conclusão ausente — adicionado (via `name="success"`; **`checkCircle` não existe** no mapa de `Icon.tsx`, conferido antes de escrever). Mesmo `moduleProgressPercent={75}` literal. Screenshot impossível — ver pendência #1. |
| 2026-08-25 | D. Conquista (Achievement) | `apps/web/.../[lessonId]/conquista/page.tsx` | Auditada — corrigida | **Bug confirmado no console:** `router.replace("/")` era chamado no corpo do render quando falta `?type=` → `Cannot update a component (Router) while rendering a different component`; movido pra `useEffect`, revalidado (0 erros no console). **Legibilidade:** o conteúdo flutuava direto sobre a grade blueprint animada do `<body>` e os traços do fundo cruzavam as palavras ("nenhuma questão" ficou atravessada por um triângulo no screenshot) — envolvido num painel opaco, que também é o que a referência do Stitch faz. **Ícone:** o glifo da medalha renderizava a 24px dentro de um badge de 112px (`text-6xl` ignorado — ver pendência de cascade layer em `PENDENCIAS_WEB_REAL.md`); trocado por `size={56}`. **Paridade:** o web somava XP/gemas no cliente mesmo com backend real, enquanto o mobile já buscava o perfil atualizado — portado o caminho `isResourceReal("gamification")` do mobile (a regra "nunca calcular XP no cliente" é do `CLAUDE.md`). **Ideal:** com o painel, a tela ficou perto da referência; segue diferente na forma do selo (losango vs hexágono do Stitch) e sem o agrupador "Recompensas" — julgados cosméticos, não corrigidos. |
| 2026-08-25 | D. Conquista (Achievement) | `apps/mobile/.../[lessonId]/conquista.tsx` | Auditada por código — **verificação visual pendente** | Mesmo `router.replace` no corpo do render — corrigido. Mesmo painel opaco adicionado por paridade (aqui não há fundo blueprint, então é fidelidade ao Stitch, não legibilidade). Screenshot impossível — ver pendência #1. |
| 2026-08-25 | E. Sem Vidas (No Hearts) | `apps/web/components/features/gamification/NoHeartsDialog.tsx` | Auditada — corrigida | **Botão desabilitado sem motivo:** com vidas cheias **ou** com gemas de menos o botão "Restaurar com 350 Gemas" só ficava apagado e nada dizia qual dos dois era o caso — agora explica ("Suas vidas já estão cheias…" / "Faltam N gemas…") e desabilita também no caso de vidas cheias, que antes só era barrado por acidente (o usuário demo tem 340 gemas de 350). Ícone de gema adicionado ao botão (Stitch tem). Coração do topo estava em 24px dentro de um círculo de 80px → `size={44}`. **Cobertura de estado:** o estado `blocked` ("Projetos em pausa!"), que é o que a referência do Stitch mostra, **não pôde ser fotografado** — nenhuma conta mock tem `hearts_current: 0`; julgado por código. **Ideal:** o texto de motivo abaixo de um botão apagado resolve a compreensão, mas o botão em si segue ilegível (mesmo `disabled:opacity-50` da tela B). |
| 2026-08-25 | E. Sem Vidas (No Hearts) | `apps/mobile/components/features/gamification/NoHeartsDialog.tsx` | Auditada por código — **verificação visual pendente** | Mesmas duas correções (motivo do botão desabilitado + ícone de gema no botão). Screenshot impossível — ver pendência #1. |
| 2026-08-25 | F/G. Explorar + Upload de Material | `apps/web/src/app/(shell)/explorar/page.tsx` + `components/features/explore/` | Auditada — corrigida | **Bug medido:** em 390px o nome do arquivo em "Meus Materiais" ficava com **77px** para um texto de 281px (`Norma_d…`) porque dividia a linha com o badge "Pronto para revisão" — badge desce pra segunda linha abaixo de `sm`; remedido: **242px**. **Estado vazio ausente:** buscar algo sem correspondência deixava "Trilhas Recomendadas" como um título seguido de nada (só "Meus Materiais" tinha mensagem) — adicionada. **Falso-positivo desmentido:** a bottom nav "atravessando o conteúdo" no screenshot `fullPage` é artefato de elemento `fixed`, confirmado normal em screenshot de viewport. **Divergência do Stitch não corrigida (ideal):** os cards de "Trilhas Recomendadas" da referência têm uma ilustração blueprint por trilha; a implementação tem só um ícone pequeno, e é a maior perda de apelo visual da tela — depende de assets que não existem no repositório. Também falta o "VER TODAS" do cabeçalho da referência. **Verificado e correto:** o badge "Em construção" do Modo Infinito não é elemento morto — é `hasContent:false` do tópico selecionado, evitando cair no `TOPIC_HAS_NO_QUESTIONS` do backend. |
| 2026-08-25 | F/G. Explorar + Upload de Material | `apps/mobile/src/app/(tabs)/explorar.tsx` + `components/features/explore/` | Auditada por código — **verificação visual pendente** | Mesmas duas correções (badge do material na segunda linha; estado vazio das trilhas). Screenshot impossível — ver pendência #1. |
| 2026-08-25 | L. Notificações | `apps/web/src/components/features/notifications/NotificationItem.tsx` + `lib/gamification/currentLesson.ts` + `lib/utils/format.ts` | Auditada — corrigida (**linha reconstruída**, ver pendência #6) | **Bug real corrigido:** `findCurrentLessonHref` (usado pra linkar a notificação até a lição sugerida) não tratava falha de `listTrackLessons` — uma trilha sem lições rejeitava o `Promise.all` inteiro e a tela inteira de Notificações caía sem exibir nada; agora cada chamada leva `.catch(() => ({ data: [] }))` e a função tem try/catch externo. **Fidelidade Stitch:** cada tipo de notificação ganhou um "tile" de fundo tingido atrás do glifo (cor semântica, ex. laranja-erro pra streak em risco) — antes era só o ícone solto; adicionado timestamp relativo ("2h atrás", nova `formatRelativeTime`, espelhada no mobile). **Acessibilidade:** `accessibilityLabel`/rótulo de tempo agora incluem o tempo relativo; foco visível adicionado ao link. |
| 2026-08-25 | L. Notificações | `apps/mobile/src/components/features/notifications/NotificationItem.tsx` + equivalentes | Auditada por código — corrigida (**linha reconstruída**, ver pendência #6) | Mesmas correções do lado web (bug do `currentLesson`, tiles de cor por tipo, timestamp relativo, label de acessibilidade). Screenshot impossível — ver pendência #1. |
| 2026-08-25 | J. Perfil | `apps/web/src/components/features/profile/AchievementGrid.tsx` | Auditada — corrigida, **verificação parcial** (ver pendência #6) | **Bug de densidade confirmado ao vivo pelo próprio agente antes de parar:** renderizar as 44 conquistas do catálogo de uma vez (maioria "Bloqueada", idênticas) inflava a altura da página — medido em **3455px**; com o grid recolhido a 9 (desbloqueadas primeiro) + botão "Ver todas (44)", caiu pra **2303px** (essa medição tem confirmação visual real do agente, ao contrário do resto desta linha). Contador "X de Y" adicionado ao cabeçalho. **Pendente:** o agente sinalizou uma "stubby orange bar" no header do Perfil antes de bater o limite de sessão — não investigado. |
| 2026-08-25 | J. Perfil | `apps/mobile/src/components/features/profile/AchievementGrid.tsx` | Auditada por código — corrigida (**linha reconstruída**, ver pendência #6) | Mesmo recolhimento (9 + "Ver todas"), mesmo contador "X de Y". Screenshot impossível — ver pendência #1. |
| 2026-08-25 | K. Loja | `apps/web/src/components/features/shop/ShopCosmeticItem.tsx` | Auditada — corrigida (**linha reconstruída**, ver pendência #6) | **Fidelidade Stitch:** botão de compra de cosmético trocado de texto azul solto pra pílula contornada de verdade (`Button variant="ghost"`), igual ao Stitch e igual aos itens utilitários da mesma tela que já usavam `Button`. Removido `interactive` do `Card` (não tinha `onClick`, virava foco morto sem ação). |
| 2026-08-25 | K. Loja | `apps/mobile/src/components/features/shop/ShopCosmeticItem.tsx` | Auditada por código — corrigida (**linha reconstruída**, ver pendência #6) | Mesma pílula contornada (estilo manual — RN não tem o componente `Button` do web aqui), estado desabilitado agora visualmente diferenciado (`purchaseButtonDisabled`), `accessibilityLabel` explícito adicionado. Screenshot impossível — ver pendência #1. |

**Ficaram de fora da rodada 2** (para a rodada 3): H. Revisão de Perguntas, I. Liga — nenhum arquivo dessas telas aparece no diff da rodada.

### Rodada 3 (2026-08-25) — H, I, M, Modo Infinito (sessão + resumo)

| Data | Tela | Alvo | Status | Achados / ação |
|---|---|---|---|---|
| 2026-08-25 | H. Revisão de Perguntas | `apps/web/src/app/(teacher)/revisao/[uploadId]/page.tsx` + `components/features/teacherReview/` | Auditada — corrigida | **Bug funcional medido (o mais grave da rodada):** o `ReviewProgressFooter` é `sticky bottom-0` com `z` auto, e a `TeacherBottomNav` é `fixed bottom-0 z-40` com 86px de altura — medido em 390px, rodapé em **y 756–844** e nav em **y 758–844**, ou seja, a barra de progresso da revisão e o botão **"Publicar Trilha" (a ação terminal da tela) ficavam 100% cobertos pela nav no telefone**, com uma fresta de 2px. Corrigido com `bottom-[86px] md:bottom-0 z-30` (acima de `md` a nav é `md:hidden` e o rodapé volta pro `bottom-0`). **Hierarquia invertida:** "Rejeitar" usava `variant="danger"` sólido e era o botão mais pesado do card, acima de "Aprovar" — a ação destrutiva chamando mais que a ação esperada da maioria da fila, e divergindo da referência do Stitch (que usa rejeitar contornado). Criada a variante **aditiva** `danger-ghost` no `Button` (não altera nenhum botão existente do app) e aplicada. **Cor como único portador de significado:** a resposta correta era sinalizada só pelo preenchimento verde — e o mesmo verde (`tone="tertiary"`) já era usado no mesmo card pelo selo de dificuldade "Fácil", então a cor sozinha nem era sinal confiável; adicionado ícone `check_circle` + `sr-only "Resposta correta"`. **Layout:** em 390px o selo "12 perguntas na fila" quebrava o próprio texto em 2 linhas e ficava com o dobro da altura dos vizinhos — `flex-wrap` no container + `whitespace-nowrap` no selo. **Verificado e correto (não é achado):** ordenação da fila e link "Revisar" funcionam de verdade. **Ideal:** a referência do Stitch mostra os contadores como numerais grandes ("03 Rejeitadas | 09 Aprovadas") em vez de três selos pequenos — mantido o formato atual (os selos escalam melhor com o status "editada e aprovada", que a referência não prevê). Tela só existe no web (Painel do Professor é web-only por decisão de `Docs/CLAUDE.md`) — sem obrigação de paridade. **Achado pela recaptura:** com o rodapé finalmente visível em 390px, o rótulo "0/12 perguntas revisadas" quebrava em duas linhas dentro da barra (sobravam ~150px ao lado do botão) — rodapé empilhado abaixo de `sm`, o que também dá a "Publicar Trilha" um alvo de toque de largura inteira. Corrigido também o item desabilitado "Turmas / Em breve" da `TeacherBottomNav`, que somava `opacity-60` sobre `text-outline` e caía bem abaixo do piso de contraste. **Reconferido na recaptura:** rodapé acima da nav, rótulo em uma linha, "Rejeitar" contornado e "Aprovar" sólido, check na resposta correta. |
| 2026-08-25 | I. Liga | `apps/web/src/app/(shell)/liga/page.tsx` + `components/features/league/LeagueHeader.tsx` + `lib/gamification/leagueTiers.ts` | Auditada — corrigida | **Elemento decorativo + drift de paridade confirmado:** o "Tempo Restante" do web era a string literal **`"2d 14h 32m"`**, sem estado nem timer — falsa precisão numa tela cuja premissa inteira é a urgência do ciclo semanal. O **mobile já tinha corrigido isso em 18/08/2026** (um `/impeccable critique` anterior) e o web ficou pra trás: portadas `weekReferenceEndsAtIso()` (`leagueTiers.ts`) e `formatDaysHoursMinutes()` (`utils/format.ts`) do mobile, ligadas ao `useCountdownToTimestamp` que o web já tinha. Agora conta pra baixo de verdade a partir de `league.week_reference`, e o bloco só renderiza quando há data válida. **Ícone medido:** o selo da liga é um círculo de 96px com o glifo em **24px** — `className="text-5xl"` não tem efeito nenhum (folha do Material Symbols fora de cascade layer, ver `PENDENCIAS_WEB_REAL.md`); trocado por `size={48}`, mesma saída usada nas telas C/D/E. **Ideal:** os 10 tiers se distinguem só pelo glifo — o selo é sempre o mesmo círculo laranja, então promover de Bronze pra Prata não dá recompensa visual nenhuma; e a trilha de progressão corta no meio da 3ª liga em 390px sem nenhuma affordance de rolagem. Nenhum dos dois foi corrigido (é decisão de design de gamificação, não defeito) — ver pendência #8. **Achado pela própria recaptura (Passo 6), não pela inspeção inicial:** com a contagem real no lugar, a tela passou a mostrar **"Tempo Restante 0m"** — duas causas somadas, ambas corrigidas: (1) o mock `week_reference` era a string fixa `"2026-W32"`, três semanas no passado, então toda demonstração nascia com o ciclo já encerrado (passou a derivar da semana ISO corrente, nos dois apps); (2) **nenhum dos dois apps tinha estado para "ciclo já fechado"** — `formatDaysHoursMinutes(0)` devolve `"0m"`, que lê como "acabou agora", para sempre. Isso não era só artefato do mock: `weekReferenceEndsAtIso` é uma aproximação em UTC e o job real de fechamento roda no fuso do usuário, então usuários reais atingem essa janela toda semana. Tratado com `reachedZero` → "Ciclo / Encerrando…" no web **e** no mobile (onde o defeito era latente desde 18/08/2026 e nunca tinha sido visto, por não haver screenshot possível). **Reconferido na recaptura:** "Tempo Restante 5d 9h 57m", selo com o escudo em 48px. |
| 2026-08-25 | I. Liga | `apps/mobile/src/app/(tabs)/liga.tsx` + `components/features/league/` | Auditada **parcialmente** por código — **incompleta, retomar na rodada 4** | Inspecionado só o **cabeçalho**, que é onde estavam os defeitos do web: o mobile **já estava correto** nos dois pontos (contagem regressiva real via `useCountdownToTimestamp(weekReferenceEndsAtIso(...))` — foi daqui que o web foi portado — e selo com `size={44}` explícito, imune ao bug de cascade que só existe no web). **Nenhuma mudança foi necessária no cabeçalho.** Os componentes `LeagueRankRow`/`LeagueRankingList`/`LeagueZoneBanner`/`LeagueTiersDialog` do mobile **não foram inspecionados** e não há screenshot (bloqueado pela pendência #1). Parity nit registrado e aceito: selo de 96px com ícone de 44 no mobile vs 48 no web. |
| 2026-08-25 | M. Painel do Professor | `apps/web/src/app/(teacher)/painel/page.tsx` + `components/features/teacherDashboard/` | Auditada — corrigida, **1 pergunta aberta** | **Elemento decorativo (pergunta #9 abaixo):** o botão **"Revisar Módulo"** de cada tópico fraco (`WeakTopicsList.tsx`) **não tem `onClick` nenhum** — promete uma ação e não faz nada. Fora do escopo de uma correção de polish porque há mais de um destino razoável; **não decidido sozinho**. **Semântica de cor invertida:** a lista mostrava `{accuracy_rate}% de acerto` pintado de `text-error-red` — um número de *acerto* em vermelho de erro lê ao contrário (quem escaneia vê "55%" em vermelho e entende taxa de erro). Trocado pelo complemento, `{100 - accuracy_rate}% de erro`, que é a grandeza que o vermelho de fato descreve e que a referência do Stitch usa ("45% de erro"). **Gráfico que não informava:** o "Engajamento Semanal" eram 5 retângulos idênticos sem eixo, sem escala e sem número — o valor só existia no `aria-label`, então quem enxerga via menos que quem usa leitor de tela. Adicionado rótulo numérico por barra e destaque do pico em `secondary`, como a referência (barra de pico em laranja). **Ideal (não corrigido):** a referência agrupa cada seção dentro de um card branco com o título *dentro*; na implementação os títulos de seção flutuam direto sobre o fundo blueprint e as linhas da grade cruzam as letras — mitigado indiretamente pela correção de opacidade do fundo (linha abaixo), mas o agrupamento em cards continua sendo a diferença estrutural com a referência. Falta também o subtítulo "Visão geral do desempenho e engajamento das turmas" e a linha de tendência dos cards de métrica ("↑ 5% este mês"), que dá contexto a números que hoje aparecem sozinhos. Tela web-only. |
| 2026-08-25 | Modo Infinito — Resumo | `apps/web/.../infinito/[topic]/resumo/page.tsx` + `apps/mobile/.../infinito/[topic]/resumo.tsx` | Auditada — corrigida (web com screenshot, mobile por código) | **Informação ausente:** o resumo dizia só "Modo Infinito" e **nunca nomeava o tema praticado**, embora o `[topic]` esteja na própria rota — dois resumos de temas diferentes eram indistinguíveis. A referência do Stitch traz "Modo Infinito - \<tema\>" como subtítulo. Corrigido nos **dois** apps (`subtitle={\`Modo Infinito · ${getThemeByTopic(topic).label}\`}`). **Ideal:** fora isso a tela está no ponto — painel opaco (não sofre do problema de legibilidade das telas vizinhas), 3 stat cards, análise e 3 ações com hierarquia correta. Duas divergências do Stitch julgadas e **mantidas de propósito**: (1) não há o selo/medalha circular no topo que a referência tem — a tela C ganhou um equivalente na rodada 1 e o mesmo argumento vale aqui, mas o título já muda pra "Nota Máxima Alcançada!" acima de 90% de precisão, que é o sinal de celebração real; (2) a ordem dos botões diverge (nosso "Voltar ao Mapa" em primário sólido, o Stitch em contorno) — a nossa hierarquia é melhor, o destino padrão deve pesar mais. |
| 2026-08-25 | Modo Infinito — Sessão | `apps/web/.../infinito/[topic]/sessao/page.tsx` + `components/features/infiniteMode/` | **Parcialmente auditada — NÃO fechar, retomar na rodada 4** | Screenshots reais capturados (390 e 1280) e inspecionados, mas **sem passe funcional (clicar o fluxo) e sem recaptura pós-correção**. **Achado principal:** o enunciado da pergunta — o texto mais importante do app — não tem superfície própria (`QuestionCard` é um container transparente) e fica direto sobre o fundo blueprint animado; no screenshot de 390px há glifos decorativos atravessando as palavras "mostre o" e "Combinando". Endereçado **indiretamente** pela correção de opacidade do fundo (linha abaixo), **não** por uma correção na tela — decidir se o enunciado merece um painel opaco é pergunta #10. **Achado não corrigido:** em 390px o título "Modo Infinito: Maquetes" quebra em **3 linhas** e espreme os selos "Nível 1" e "Dificuldade Elevada" no mesmo cabeçalho. **Não verificado:** paridade com `apps/mobile/.../infinito/[topic]/sessao.tsx` (não lido nesta rodada). |
| 2026-08-25 | *(transversal — todas as telas)* | `apps/web/src/app/globals.css` + `apps/mobile/.../AnimatedBlueprintBackground.tsx` | Corrigida | **Erro de portagem medido, não estético:** o mockup de referência (`fundo-animado-blueprint-v2.html`) usa opacidade 0.6/0.55/0.5 **com cores dessaturadas próprias da camada decorativa** (`#2E5C8A`, `#C77B3B`). A portagem trocou essas cores pelos tokens de marca em força total (`--color-primary #0E4471`, `--color-secondary #8F4E0F`) e **manteve a opacidade do mockup** — a decoração ficou mais escura que a própria referência. Medido sobre `#F8F9FF`: azul do mockup a 0.6 = **2.75:1**; nosso `--color-primary` a 0.6 = **3.34:1**, ou seja, passava do piso de **3:1** que a WCAG usa pra conteúdo não-textual **com significado** — decoração tão forte quanto um elemento que carrega informação. Baixado pra 0.5/0.45/0.4 nos **dois** apps (volta a 2.64:1, o peso que a referência sempre quis). Afeta todas as telas, sempre no sentido de deixar o fundo mais quieto — nenhuma tela já auditada fica pior. |

### Rodada 4 (2026-08-25) — Resumo Inteligente, Chat sobre Material, Modo Infinito (fechamento), Liga mobile

| Data | Tela | Alvo | Status | Achados / ação |
|---|---|---|---|---|
| 2026-08-25 | N. Resumo Inteligente | `apps/web/.../materiais/[uploadId]/resumo/page.tsx` + `components/features/materialSummary/` | Auditada — corrigida | **Legibilidade (achado principal, medido em screenshot a 390 e 1280):** esta é a tela de **leitura mais longa do app** e todo o corpo — sinopse e os 3 pontos-chave — ficava direto sobre a grade blueprint animada do `<body>`. Nos screenshots as linhas da grade cruzam **todas** as linhas de texto e glifos decorativos (esquadro, régua) atravessam as palavras "estrutural e de vedação" e "livremente. Na alvenaria de vedação". Diferente da pendência #10 (enunciado do quiz, ainda em aberto), **aqui a referência do Stitch já responde**: `resumo_simplificado_sistemas_construtivos/screen.png` agrupa os pontos-chave dentro de um card branco. Corrigido: `KeyPointsChecklist` virou um `Card` com divisores de 2px entre os pontos, e a sinopse ganhou um `Card bordered={false}` (superfície sem virar mais uma "caixa"). **Cor com dois empregos:** o marcador de cada ponto-chave era `check_circle` **verde preenchido** (`text-tertiary`) — mas verde é "exclusivamente sucesso/validação" pela One Job Per Color Rule (`apps/web/DESIGN.md`), e um check verde ao lado de "Modulação" também lê como "você já domina isto", o que é falso. Trocado por check contornado em `text-primary` (marcador estrutural de lista). Nota: o Stitch usa laranja aqui, mas laranja é "exclusivamente gamificação" na regra escrita — a regra escrita vence a tela, mesmo precedente da rodada 1. **Fidelidade:** adicionado o glifo `fact_check` ao lado de "O que você precisa saber" (Stitch tem), o ícone `forum` dentro do botão "Tirar Dúvidas" e a legenda "Converse com o assistente sobre este documento" abaixo dele (ambos na referência, ambos ausentes). **Divergência do Stitch julgada e mantida:** botão em azul e não laranja (mesma regra acima; mesmo precedente da tela C). **Ideal (não corrigido):** `summary.generated_at` existe no contrato e no mock e **nunca é exibido** — um resumo gerado por IA sem nenhuma indicação de que é gerado nem de quando é uma lacuna de confiança, mas adicionar o carimbo não tem respaldo na referência e mexe na hierarquia do topo; fica registrado. O `DiagramCard` segue um placeholder honesto (~290px de altura sem entregar imagem) porque o pipeline que geraria o diagrama ainda é stub no backend. **Passe funcional ao vivo:** navegação "Tirar Dúvidas" → `/chat` e botão "Voltar" do header confirmados no Playwright; 0 erros de console. **Reconferido na recaptura (390px):** sinopse e pontos-chave sobre superfície limpa, divisores entre os 3 pontos, checks contornados em azul, `fact_check` no título, ícone e legenda no CTA — nenhuma linha de texto do corpo é mais cruzada pela grade. Único elemento que segue sobre o fundo é o próprio título de seção (display 24px, legível; a referência faz igual). |
| 2026-08-25 | N. Resumo Inteligente | `apps/mobile/src/app/materiais/[uploadId]/resumo.tsx` + `components/features/materialSummary/` | Auditada por código — corrigida, **verificação visual pendente** | Mesmas 5 correções por paridade: `Card` nos pontos-chave (com divisor manual `borderTopWidth`, RN não tem `divide-y`), `Card bordered={false}` na sinopse, marcador `taskAlt` (= `check-circle-outline`) em `colors.primary` no lugar de `success` em `colors.tertiary`, `factCheck` no título da seção, ícone `forum` + legenda no CTA. Os três nomes de ícone (`taskAlt`, `factCheck`, `forum`) foram **conferidos no mapa de `Icon.tsx` antes de escrever** — nenhum foi assumido. Screenshot impossível — ver pendência #1. |
| 2026-08-25 | O. Chat sobre Material | `apps/web/.../materiais/[uploadId]/chat/page.tsx` + `components/features/materialChat/` | Auditada — corrigida | **Estado vazio ausente (achado mais grave da tela):** uma conversa nova — que é o **estado mais provável de todos**, já que todo material começa sem histórico — renderizava só o header, a barra de digitação e ~1300px de grade blueprint com rabiscos decorativos no meio. Nada dizia o que a Arq faz, nada sugeria o que perguntar. Confirmado ao vivo em `/materiais/upload-planta-baixa/chat` (esse upload não tem histórico no mock). Criado `ChatEmptyState` (web + mobile): selo, "Pergunte à Arq", explicação de que ela responde **com base naquele material e cita a página**, e 3 sugestões de pergunta. As sugestões **não são decorativas** — chamam o mesmo `handleSend` da barra de digitação. **Elemento que promete e não faz (medido, não suposto):** com o campo vazio o botão "Enviar" ficava habilitado (`isDisabled() === false` no Playwright) e o clique caía num early-return silencioso; agora `disabled={!canSend}`. **Legibilidade:** a citação do documento (`sourceExcerpt`) era um `<blockquote>` sem fundo — no screenshot pós-envio as linhas da grade e um glifo de esquadro cruzam o texto em itálico ("dimensionais durante a execução"); ganhou superfície branca com borda e faixa `primary` à esquerda, como o card "Trecho do Documento" do Stitch. **Autoria ausente:** duas bolhas sem rótulo nenhum (uma azul, uma cinza) — o Stitch rotula os dois lados ("Você" + "EXPLICAÇÃO DETALHADA"); adicionados rótulos "Você"/"Arq" (este com o glifo de lâmpada). **Ritmo:** as mensagens eram ancoradas no topo, deixando ~500px de vazio até o input mesmo com conversa em andamento — a decoração de fundo virava o elemento dominante; agora `justify-end` (e `justify-center` no estado vazio). **Robustez:** `getUploadSummary`/`listChatHistory` não tinham `.catch` — uma falha virava unhandled rejection e a tela ficava muda (mesma classe de defeito corrigida em Notificações na rodada 2); agora falham com mensagem. **Passe funcional ao vivo:** enviar "Como funciona a modulação?" retorna a resposta certa, com o trecho citado e "Página 14", e o input é limpo só no sucesso; 0 erros de console. **Reconferido na recaptura (390px, dois estados):** conversa nova mostra o painel "Pergunte à Arq" centralizado com as 3 sugestões; conversa em andamento mostra os rótulos "VOCÊ"/"ARQ" e as bolhas ancoradas na barra de digitação. Nenhuma regressão nova introduzida pelo lote. |
| 2026-08-25 | O. Chat sobre Material | `apps/mobile/src/app/materiais/[uploadId]/chat.tsx` + `components/features/materialChat/` | Auditada por código — corrigida, **verificação visual pendente** | Mesmas 6 correções por paridade: `ChatEmptyState` espelhado (via `ListEmptyComponent` do `FlatList`, com as mesmas 3 sugestões e a mesma cópia), botão de enviar desabilitado com campo vazio (e o glifo em `colors.outline` quando inativo, já que RN não tem `disabled:opacity` automático), superfície na citação, rótulos de autoria, `contentContainerStyle` com `flexGrow: 1` + `justifyContent` condicional (equivalente RN do `justify-end`/`justify-center`), e os dois `.catch` em `useMaterialChat`. Screenshot impossível — ver pendência #1. |

| 2026-08-25 | Modo Infinito — Sessão | `apps/web/.../infinito/[topic]/sessao/page.tsx` + `components/features/infiniteMode/` | **Fechada** (era parcial na rodada 3) | **Bug funcional achado no passe funcional — o mais grave da rodada, e invisível numa inspeção só visual:** `continueNext()` faz `setLastResult(null)` ao avançar de pergunta, e **`levelProgress`, `level` e `questionsAnswered` eram todos derivados de `lastResult`**. Resultado: o cabeçalho desabava para **"Nível 1" e "0/20" enquanto o usuário lia a pergunta seguinte** — ou seja, durante quase toda a sessão; os valores certos piscavam apenas nos poucos segundos entre "Confirmar" e "Continuar". Medido ao vivo no Playwright: responder 1 pergunta corretamente e clicar "Continuar" deixava o header em `0/20`, enquanto a sessão encerrada reportava `questions=1&correct=1`. Numa tela cuja premissa é "quantas você aguenta seguidas", o único medidor de progresso mostrava zero permanentemente. Corrigido com acumuladores próprios (`answeredCount`/`levelCurrent`) que não são limpos ao avançar. **Reverificado ao vivo:** header passa a `1/20` após avançar. **Título de 3 linhas (pendência da rodada 3) corrigido:** os selos "Nível N" e "Dificuldade Elevada" desceram para linha própria abaixo de `sm`; medido depois: título em **298×24px, uma linha**, header total 130px. **Resto do passe funcional (tudo correto, não é achado):** "Confirmar" nasce desabilitado e habilita ao escolher opção; "Desistir" encerra a sessão de verdade e leva ao resumo com os parâmetros certos; as opções usam `role="radio"` dentro de um `role="radiogroup"` (ARIA correta nos dois apps — meu seletor inicial é que estava errado); 0 erros de console. **Segue em aberto (não é meu para decidir):** o enunciado sem superfície própria — pendência #10. |
| 2026-08-25 | Modo Infinito — Sessão | `apps/mobile/.../infinito/[topic]/sessao.tsx` + `components/features/infiniteMode/` | Auditada por código (1ª leitura) — corrigida, **verificação visual pendente** | **Mesmo bug do contador/nível** (`useInfiniteModeSession.ts` linha a linha idêntico ao web) — corrigido igual. **Variação do defeito do título:** aqui o `<Text>` tem `numberOfLines={1}`, então em vez de quebrar em 3 linhas como o web, ele **truncava** — dividindo a linha com o botão de fechar e os dois selos sobravam ~90px e o nome do tema, que é o assunto da tela, sumia atrás das reticências. Selos movidos para linha própria (sempre, já que largura de telefone é sempre estreita; o web só faz isso abaixo de `sm`). Screenshot impossível — ver pendência #1. |
| 2026-08-25 | I. Liga — Ranking/`LeagueTiersDialog` | `apps/web/.../league/LeagueTiersDialog.tsx` + `apps/mobile/.../league/{LeagueRankRow,LeagueTiersDialog}.tsx` | Auditada — corrigida (**linha escrita pela sessão orquestradora**, ver nota) | Fecha o item que ficara incompleto na rodada 3 (só o cabeçalho da Liga tinha sido inspecionado). **Bug real (idêntico nos dois apps):** o fetch de ranking ao trocar de liga/divisão no `LeagueTiersDialog` não tinha `.catch` — uma falha de rede caía no mesmo ramo do estado genuinamente vazio, e a tela afirmava **"Ninguém está na \<liga\> ainda esta semana"**, uma informação falsa sobre o mundo em vez de um aviso de erro (mesma classe de defeito já corrigida em Notificações na rodada 2). Adicionado estado `loadError` com mensagem própria (`role="alert"` no web, `accessibilityLiveRegion="polite"` no mobile). **Drift de paridade no mobile:** `LeagueRankRow` renderizava posição e XP semanal em `bodySm` (Inter) — o web já usa a família mono pra número que o usuário acompanha subir (Mono-Counts Rule, `DESIGN.md`); criado o token `statsNumSm` (`theme/tokens.ts`, mesma família/composição do `statsNum` já existente, só no tamanho de uma linha de tabela) e aplicado. **Ideal:** com esses dois corrigidos, o ranking do mobile fica no mesmo padrão do cabeçalho (já correto desde a rodada 3) — `LeagueZoneBanner` foi lido de passagem e não apresentou problema. **Nota de proveniência:** as edições de código são do próprio `ui-reviewer` (confirmadas por diff real, mesmo padrão de verificação das linhas "reconstruídas" da pendência #6), mas a sessão orquestradora escreveu esta linha do log porque a rodada 4 encerrou antes de chegar ao Passo 7 para este item — sem inspeção visual (mobile já bloqueado pela pendência #1; web não capturado nesta rodada). |

### Rodada 5 (2026-08-25) — verificação visual/funcional das 5 decisões de produto (pendências #2/#3/#8/#9/#10)

Modo demanda, não varredura: a sessão orquestradora implementou as 5 decisões e verificou por tipo
(`tsc --noEmit`, `go build/vet/gofmt` limpos); esta rodada é a verificação ao vivo com screenshot
que só o `ui-reviewer` faz. Todos os screenshots do web foram capturados com a técnica da pendência
#4 (cookie `arqlearn_mock_account` + `NEXT_PUBLIC_API_REAL_RESOURCES` vazia) — inclusive o Painel,
que exige a conta **`teacher-marina`** e não `student-alex` (`/painel` redireciona pra Home com a
conta de aluno; anotado aqui porque custou uma rodada de captura perdida).

| Data | Tela | Alvo | Status | Achados / ação |
|---|---|---|---|---|
| 2026-08-25 | A. Home — card da unidade (pendência #2) | `services/monolith/internal/learning/learning.go` + `apps/web/.../home/UnitSection.tsx` + `apps/mobile/.../home/UnitSection.tsx` | Verificada — sem regressão, **2 documentos corrigidos** | **Sem efeito visual, como esperado:** a conta demo está em `track-fundamentos`, que não tem `description` no mock — o card de subtítulo simplesmente não é renderizado. Confirmado ao vivo que isso é o comportamento *correto* e não uma quebra: `{subtitle && ...}` guarda a caixa nos **dois** apps (web `UnitSection.tsx:57`, mobile `UnitSection.tsx:60`), o DOM não contém a string `"undefined"` e o console fica em 0 erros. Trilha sem descrição não deixa card vazio. **Sync dos documentos conferido:** API Spec §3.3 e Database Design §4.1 batem com o struct Go (`omitempty`, omitido quando vazio, nunca `null`). **Dois documentos ficaram contradizendo o próprio código depois da mudança — corrigidos aqui:** (1) `Docs/CLAUDE.md`, pegadinha (3), ainda afirmava que o campo NÃO está no struct/API Spec/Database Design e que "não esperar que apareça em GET /v1/tracks até alguém formalizar nos três lugares" — exatamente o que acabou de ser feito; reescrita como **[RESOLVIDO em 25/08/2026]** no mesmo molde da pegadinha (4). (2) O comentário de `apps/web/.../UnitSection.tsx` dizia "`track.description` (...) hoje NÃO é serializado pela API real — na prática o card não aparece até alguém decidir o que deve morar nessa caixa"; atualizado. **Ideal:** a única trilha do mock com `description` é `track-historia`, e o texto dela ("Grécia e Roma Antiga") é **idêntico ao título da 1ª lição** — se essa vier a ser a trilha atual de alguém, o card de subtítulo repete o que o nó logo abaixo já diz. Vale escolher descrições que acrescentem informação, não que dupliquem uma lição. |
| 2026-08-25 | C. Resumo da Lição — progresso real (pendência #3) | `apps/web/.../[lessonId]/resumo/page.tsx` + `apps/mobile/.../[lessonId]/resumo.tsx` | Verificada ao vivo — **correta** | **Medido em duas trilhas com resultados diferentes, que é o teste que desmente o "75% fixo":** `track-historia/t2-l1` → **50%** (2 lições, a da rota conta como concluída) e `track-fundamentos/t1-l1` → **100%** (3/3 concluídas). Screenshot confere: a barra em 50% está preenchida até a metade, não é só o rótulo que muda. Sem flash de erro nem estado quebrado durante o carregamento — começa em 0% e assume o valor real (o `.catch` degrada pra 0% em vez de inventar número). 0 erros de console. Mobile é linha a linha idêntico ao web (mesmo `useEffect`, mesmo filtro, mesmo `.catch`) — paridade correta, screenshot impossível (pendência #1). **Ideal:** o rótulo continua "Progresso do módulo" enquanto o número é o da **trilha inteira** (a API não expõe unidade por lição) — a nota de escopo já está registrada na pendência #3, mas o rótulo em si segue prometendo uma granularidade que o dado não tem. Renomear pra "Progresso da trilha" fecharia a lacuna sem mexer em backend. |
| 2026-08-25 | I. Liga — cor por tier (pendência #8) | `apps/web/.../league/{LeagueHeader,LeagueTiersDialog,LeagueProgressionTrack}.tsx` + `apps/mobile/` equivalentes | Auditada — **corrigida (1 defeito real achado)** | **Selo grande e seletor: corretos.** Selo do cabeçalho medido ao vivo em Bronze: `bg rgb(156,90,46)` (#9c5a2e) com o glifo em `rgb(252,231,214)` (#fce7d6) a **48px** — **4.48:1** de contraste, folgado pro piso de 3:1 de conteúdo não-textual. Navegados 3 tiers no seletor conforme pedido: Madeira `#7c5430`/`#f5e8d8`, Ouro `#c99a2e`/`#2e2000`, Safira `#235fa8`/`#e8f1ff` — cada um acende na própria cor, os outros 9 seguem `bg-surface-gray` neutro, e o glifo continua legível em todos (o par bg/on inverte pra texto escuro no Ouro, que é o caso claro). **Defeito real encontrado, não pedido:** a `LeagueProgressionTrack` — que fica **na mesma dobra, ~200px abaixo do selo** — continuava pintando o tier atual em `bg-primary`. Medido: selo Bronze marrom `rgb(156,90,46)` e nó "Bronze" da trilha **azul**, o mesmo tier em duas cores na mesma tela. Isso esvazia justamente o objetivo da pendência #8 (tornar a promoção visível). Corrigido nos **dois** apps com o mesmo `LEAGUE_TIER_BG_CLASS`/`_ON_CLASS` (web) e `LEAGUE_TIER_COLOR_KEYS` (mobile); `badgeActive` virou código morto no mobile e foi removido. **Reconferido na recaptura:** nó ativo `rgb(156,90,46)` = idêntico ao selo. **Decisão de projeto tomada e documentada:** o **rótulo de texto** do nó continua em `text-primary`/`colors.primary` de propósito — a paleta de tier é de *material* e `prata`/`platina`/`diamante` são claríssimos, então não passariam contraste como texto sobre o card branco; o círculo carrega o material, o rótulo carrega o estado. **Sync de token conferido valor a valor:** os 20 hex novos batem exatamente entre `globals.css` e `tokens.ts`. **Divergência de documentação corrigida:** os 20 tokens não existiam em nenhum `DESIGN.md`, e três deles (`esmeralda`/`safira`/`rubi` = verde/azul/vermelho) colidem de frente com a One Job Per Color Rule; adicionada a seção "The League Palette Exception" em `apps/web/DESIGN.md` fixando que é paleta de material, quais são os 3 lugares onde pode ser usada, e que nunca serve pra texto. **Ideal (não corrigido):** o seletor de 10 tiers é `overflow-x-auto` e em 390px o 10º (Diamante) fica cortado sem nenhuma affordance de rolagem — mesmo problema já registrado na rodada 3 pra trilha de progressão. |
| 2026-08-25 | M. Painel do Professor — "Revisar Módulo" (pendência #9) | `apps/web/(teacher)/painel/page.tsx` + `teacherDashboard/{WeakTopicsList,ReviewQueueTable}.tsx` + `mocks/fixtures/teacherAnalytics.ts` | Auditada — **corrigida (o caminho feliz era inalcançável)** | **Mecânica: correta.** Passe funcional ao vivo com `teacher-marina`: clicar "Revisar Módulo" rola até a fila, aplica o filtro, mostra o chip com o nome do tópico + "×", e limpar o chip restaura as 3 linhas. Trocar de turma reseta o filtro. 0 erros de console. A troca de `{accuracy_rate}% de acerto` (vermelho) por `{100-accuracy_rate}% de erro` confere na tela ("45% de erro", "38% de erro"). **Defeito real, achado antes da captura e confirmado ao vivo:** os `weak_topics` do mock eram "Cálculo de Fundações"/"Escala e Proporção"/"Sistemas Estruturais"/"Conforto Térmico" e **todas** as linhas de `mockReviewQueue` eram "Introdução ao BIM" — ou seja, **nenhum tópico fraco existia na fila**, e portanto *todo* clique em "Revisar Módulo" caía no estado vazio. O recurso que o usuário acabou de aprovar era literalmente indemonstrável: só o caminho de exceção era alcançável. Corrigido em `teacherAnalytics.ts` fazendo cada turma ter **um tópico COM questões na fila** (caminho feliz) **e um SEM** (estado vazio) — os dois casos continuam testáveis de propósito, com comentário explicando que os dois `topic` são a chave que liga as duas seções e têm que casar. **Reverificado na recaptura:** 1º tópico → chip + as 3 linhas da tabela; 2º tópico → mensagem de vazio nomeando o tópico ("Nenhuma questão pendente de 'Escala e Proporção' — as outras questões da turma continuam na fila."), que era o caso de borda pedido. **Ideal (não corrigido, registrado em PENDENCIAS_WEB_REAL.md):** (1) a fila inteira tem um único tópico, então filtrar não *estreita* visivelmente a tabela — só dá pra provar pelo estado vazio; incluir uma linha de um segundo tópico exigiria um segundo upload em `mockReviewQuestions`, senão o link "Revisar" quebra. (2) Em 390px a `ReviewQueueTable` transborda: a coluna "Ação" (o link **"Revisar"**, que é a ação da linha) fica 100% fora da tela. O wrapper tem `overflow-x-auto`, então é alcançável por rolagem horizontal — mas sem nenhuma affordance, e agora **mais grave**, porque o novo "Revisar Módulo" leva o professor direto pra essa tabela. Não corrigido aqui: escolher entre layout de card no telefone e dica de rolagem é decisão de design, não correção. |
| 2026-08-25 | B + Modo Infinito — painel opaco no enunciado (pendência #10) | `apps/web/.../quiz/QuestionCard.tsx` + `apps/mobile/.../quiz/QuestionCard.tsx` | Verificada ao vivo — **correta** | Verificado nas **duas** telas que compartilham o componente, como pedido. **Quiz da lição** (`track-fundamentos/t1-l1/sessao`) e **Modo Infinito** (`infinito/maquetes/sessao`), 390 e 1280: o selo de dificuldade + o enunciado agora ficam sobre `bg-surface-bright` com borda de 2px e `rounded-xl`, e **nenhuma linha da grade blueprint cruza mais o texto da pergunta** — que era exatamente o defeito medido na rodada 3 (glifos decorativos atravessando "mostre o" e "Combinando" no enunciado de Maquetes; no screenshot novo esse mesmo enunciado de 7 linhas está inteiro sobre superfície limpa). As opções de resposta abaixo seguem com a superfície que já tinham, visualmente distintas do painel do enunciado (borda mais fina), então a hierarquia "pergunta ≠ alternativas" ficou mais clara e não menos. 0 erros de console nas duas rotas. Mobile é a mesma moldura (`surfaceBright` + `outlineVariant` + `radius.xl`) — paridade correta, screenshot impossível (pendência #1). **Ideal:** no Modo Infinito em 390px o painel do enunciado + as 4 opções ocupam quase toda a altura útil; com um enunciado ainda mais longo o botão "Confirmar" vai exigir rolagem. Não é defeito hoje (medido: cabe), mas é a folga que sobrou. |

**Resultado da suíte:** `npm run test:visual` — **2 passed** (login do web + boas-vindas do mobile).
Como já registrado na pendência #1, isso **não** cobre nenhuma das 5 telas desta rodada: os dois
baselines são telas não autenticadas. Nenhuma das 5 ganhou teste novo — ver pendência #11.

**Detector do impeccable** (`detect.mjs` sobre os 69 arquivos de UI alterados): **7 achados, nenhum
nos 5 itens desta demanda.** Todos pré-existentes de rodadas anteriores: `border-l-4` em
`ChatMessageBubble.tsx` e `QuestionReviewCard.tsx` (adicionados de propósito nas rodadas 3–4 pra
espelhar o card "Trecho do Documento" do Stitch), `border-t-2` em `ReviewProgressFooter.tsx` e
`TeacherBottomNav.tsx` (borda superior de barra fixa, não de card arredondado — falso-positivo da
regra), e duas cores de **tema escuro** não documentadas em `tokens.ts` (`#949ca8`, `#16191d`), que
não são desta demanda. O 7º — `codex-grid-background` em `globals.css` — é falso-positivo com
evidência concreta: a grade blueprint **é** a identidade visual do produto
(`Docs/stitch_app_visual_identity/blueprint_narrative/`) e a própria regra do detector isenta
superfícies de blueprint; resolvido pela ferramenta dele
(`hook-admin.mjs ignore-rule codex-grid-background` → `.impeccable/config.json`), não silenciado à
mão. Os 20 tokens novos de liga **não** foram acusados como drift de cor.

## Pendências abertas geradas por estas auditorias

### 1. A suíte visual não cobre nenhuma tela autenticada (aberta em 2026-08-24)

Os dois testes de `e2e/visual/` se chamavam "home do apps/web" e "home do apps/mobile", mas os
baselines salvos eram a **tela de login** (web — `/` é rota protegida e redireciona) e a **tela de
boas-vindas** (mobile — sem sessão). A suíte passava verde havia versões sem nunca ter olhado para
o Mapa de Aprendizado. Os testes foram renomeados para dizer a verdade sobre o que cobrem
(`web-login.png`, `mobile-boas-vindas.png`), mas a lacuna continua:

> **Nenhuma tela de dentro do app tem cobertura de regressão visual.**

Fechar isso exige uma decisão do usuário sobre como o Playwright obtém uma sessão — por exemplo,
uma conta de teste dedicada com as credenciais em variável de ambiente (`E2E_EMAIL`/`E2E_PASSWORD`,
nunca commitadas), ou uma sessão semeada. Enquanto não houver essa decisão, **toda auditoria de
tela autenticada depende de inspeção manual** e o lado mobile não pode ser verificado visualmente
de jeito nenhum.

### 2. O que deve morar no card da unidade no Mapa — **RESOLVIDO em 2026-08-25**

Decisão do usuário: (c) formalizar `track.description` nos três lugares. Feito — `track` struct
(`services/monolith/internal/learning/learning.go`) ganhou o campo (`bson`/`json`, `omitempty`),
documentado em `ArqLearn_API_Specification.md` §3.3 (*v1.32*) e `ArqLearn_Database_Design.md` §4.1.
O frontend dos dois apps já lia `track.description` desde a rodada 1 (só o backend não devolvia) —
nenhuma mudança de UI necessária além da já feita então. `go build`/`go vet` limpos.

### 3. "Progresso do módulo" no Resumo da Lição — **RESOLVIDO em 2026-08-25**

Decisão do usuário: (a) calcular de verdade. Implementado nos dois apps
(`trilhas/[trackId]/[lessonId]/resumo`): busca `GET /v1/tracks/{track_id}/lessons` e calcula
`lições concluídas / total` da trilha. Nota de escopo: a API não expõe a qual **unidade** uma
lição pertence (`track.units` nunca é serializado — só a ordem achatada da trilha inteira), então
"módulo" aqui virou progresso da **trilha inteira**, não de uma unidade dela; formalizar o conceito
de unidade na API ficaria pra uma decisão futura, se o usuário quiser essa granularidade depois.
Falha de rede degrada pra 0% em vez de inventar um número. `tsc --noEmit` limpo nos dois apps.

### 4. O caminho que destrava cobertura visual autenticada **no web** já existe (achado em 2026-08-25)

Complemento concreto à pendência #1: no `apps/web` dá para autenticar o Playwright sem credencial
real — cookie `arqlearn_mock_account=student-alex` **mais** `NEXT_PUBLIC_API_REAL_RESOURCES` vazia no
ambiente do `next dev` (sem esvaziar a var, a conta demo cai no error boundary). Foi assim que as
telas B–G do web foram fotografadas nesta rodada. Isso **não** foi transformado em teste em
`e2e/visual/` porque o teste dependeria de o servidor ter subido com aquela variável vazia — decidir
como o CI/dev garante isso (script dedicado? `.env.test`?) continua sendo decisão do usuário. O
mobile segue sem equivalente.

### 5. `npm run test:visual` não roda numa árvore recém-clonada (achado em 2026-08-25)

`@playwright/test` está declarado no `package.json` e no `package-lock.json` da raiz, mas não estava
presente em `node_modules` — `npx playwright test` falhava com `MODULE_NOT_FOUND` ao carregar
`playwright.config.ts`. Resolvido rodando `npm install` na raiz (o lockfile não mudou, então não é
divergência de dependência — era só a instalação incompleta). Vale saber que o comando documentado
em `Docs/CLAUDE.md` depende desse `npm install` ter sido feito depois que o Playwright entrou no
projeto.

### 6. Rodada 2 interrompida por limite de sessão — linhas reconstruídas e verificação pendente (aberta em 2026-08-25)

O agente da rodada 2 (modo varredura) bateu o teto de uso da sessão (`session limit`, reset
1:20pm America/Sao_Paulo) no meio do trabalho em L/J/K, antes de escrever seu próprio relatório e
antes de completar o Passo 7. As linhas de L (Notificações), J (Perfil) e K (Loja) acima foram
**reconstruídas pela sessão orquestradora a partir do `git diff` real** (não pelo próprio
`ui-reviewer`) — o código e os achados descritos são reais e conferidos linha a linha no diff, mas
**não têm a inspeção visual (screenshot vs `screen.png` do Stitch) que o Passo 4 do agente exige**,
exceto a medição de altura do Perfil (3455px → 2303px), que o próprio agente confirmou ao vivo
antes de parar.

**Duas coisas pendentes de verificação quando a próxima rodada retomar:**
1. A "stubby orange bar" que o agente sinalizou no header do Perfil (`apps/web`) sem detalhar nem
   corrigir — provavelmente um elemento cortado/mal dimensionado, não investigado.
2. Confirmação visual geral (screenshot real, técnica do cookie mock da pendência #4) das 6 linhas
   marcadas "linha reconstruída" nesta rodada — hoje só têm garantia de que o código compila
   (`tsc --noEmit` limpo nos dois apps) e de que a lógica bate com o diff, não de que a tela
   renderiza como esperado.

**Status na rodada 3:** o item 1 (a "stubby orange bar" do header do Perfil) **não foi
investigado** — o orçamento da rodada foi consumido pelas telas novas (H/I/M/Modo Infinito). Segue
aberto para a rodada 4, junto com o item 2.

### 7. Ficaram de fora da rodada 3 (para a rodada 4)

- **Resumo Inteligente** (`materiais/[uploadId]/resumo`) e **Chat sobre Material**
  (`materiais/[uploadId]/chat`) — nunca auditadas, nos dois apps.
- **Modo Infinito — Sessão**: parcialmente auditada (ver linha acima). Faltam o passe funcional, a
  correção do título de 3 linhas em 390px e a leitura do equivalente no mobile.
- **I. Liga no mobile**: só o cabeçalho foi conferido. Faltam `LeagueRankRow`,
  `LeagueRankingList`, `LeagueZoneBanner` e `LeagueTiersDialog`.
- Os dois itens da pendência #6 acima (barra laranja do Perfil + confirmação visual de L/J/K).

### 8. O selo da Liga não recompensa a promoção — **RESOLVIDO em 2026-08-25**

Decisão do usuário: dar cor/material próprio a cada tier. Implementado nos dois apps — 10 pares
bg/on novos (`apps/web/globals.css` `--color-league-*`, `apps/mobile/theme/tokens.ts`
`league*`/`onLeague*`, mesmos hex nas duas plataformas, tema-invariantes como as cores `*Fixed` já
existentes), mapeados por tier em `leagueTiers.ts` (`LEAGUE_TIER_BG_CLASS`/`_ON_CLASS` no web,
`LEAGUE_TIER_COLOR_KEYS` no mobile). Aplicado no selo grande do cabeçalho (`LeagueHeader.tsx`/
`liga.tsx`) e no seletor de 10 tiers do `LeagueTiersDialog` (só no ícone **ativo** — o preenchimento
continua sinalizando "selecionado" como antes, agora na cor do próprio tier em vez de sempre azul/
laranja). A trilha de progressão cortando no meio da 3ª liga em 390px **não foi endereçada** — ficou
fora do pedido do usuário, registrar como possível pendência nova se incomodar na prática.

### 9. "Revisar Módulo" no Painel do Professor não faz nada — **RESOLVIDO em 2026-08-25**

Decisão do usuário: combinação das opções (a) e (b). A fila de revisão já mora na mesma página
(`painel/page.tsx`, seção "Fila de Revisão de Questões") e já carrega `topic` por linha — sem
precisar do `upload_id` em `WeakTopic` nem de uma tela nova. "Revisar Módulo" agora filtra a fila
visível por aquele tópico e rola a página até ela (`WeakTopicsList` ganhou `onReviewTopic`, estado
`reviewQueueTopicFilter` levantado pro componente pai, chip com "×" pra limpar o filtro,
`ReviewQueueTable` ganhou `emptyMessage` customizável pra não confundir "nenhuma questão deste
tópico" com "fila vazia"). Filtro reseta ao trocar de turma.

### 11. Nenhuma das 5 telas da rodada 5 ganhou cobertura de regressão visual (aberta em 2026-08-25)

Complemento operacional das pendências #1 e #4, aberto porque a rodada 5 tocou 5 superfícies e
nenhuma delas tem baseline. As 5 telas (Home, Resumo da Lição, Liga, Painel do Professor, Quiz/Modo
Infinito) são todas autenticadas; no **web** todas foram fotografadas nesta rodada sem credencial
real — o Painel inclusive, com a conta `teacher-marina` —, então o obstáculo **não é mais técnico**,
é só a decisão de como o servidor de teste garante `NEXT_PUBLIC_API_REAL_RESOURCES` vazia (pendência
#4: script dedicado? `.env.test`? `webServer` no `playwright.config.ts`, que hoje **não existe** — a
suíte assume que alguém já subiu `localhost:3000` e `localhost:8081` na mão).

Deliberadamente **não** adicionei os testes nesta rodada: um baseline que só passa quando o dev
lembrou de subir o servidor com a variável certa falha por motivo errado e ensina a equipe a ignorar
a suíte — pior que não ter o teste. Precisa da decisão do usuário sobre o mecanismo primeiro.

Vale repetir o alerta que já vale desde a rodada 1: as 5 telas desta rodada acabaram de mudar de
aparência, então **congelar baseline agora é o melhor momento** — depois de mais uma rodada de
correções o baseline nasce registrando defeito.

### 12. Duas cores de tema escuro fora do DESIGN.md (aberta em 2026-08-25)

O detector do impeccable acusa `#949ca8` (`outline`) e `#16191d` (`background`) em
`apps/mobile/src/theme/tokens.ts` como cores fora da paleta documentada. Não são desta demanda — o
`outline` inclusive carrega comentário de uma correção de contraste de 19/08/2026. A causa é que o
`DESIGN.md` documenta a paleta **clara** e o bloco `darkColors` nunca foi documentado em lugar
nenhum. Enquanto isso não for fechado, todo `detect.mjs` sobre esse arquivo vai continuar acusando
os dois — e o ruído recorrente é o que faz um achado real passar batido no meio.

### 10. O enunciado da pergunta não tem superfície própria — **RESOLVIDO em 2026-08-25**

Decisão do usuário: opção (a), painel opaco. `QuestionCard` (compartilhado pela tela B e pelo Modo
Infinito, nos dois apps) ganhou a mesma moldura já usada em Conquista/Resumo Inteligente
(`bg-surface-bright` + borda `outline-variant` + `rounded-xl`, web; `colors.surfaceBright` +
`borderColor: outlineVariant` + `radius.xl`, mobile) ao redor do selo de dificuldade + texto do
enunciado — não ao redor das opções de resposta, que já tinham superfície própria. `tsc --noEmit`
limpo nos dois apps. **Verificação visual feita na rodada 5 (25/08/2026): confirmada correta** no
web nas duas telas (Quiz da lição e Modo Infinito, 390 e 1280) — nenhuma linha da grade cruza mais o
enunciado. Mobile segue sem screenshot possível (pendência #1), verificado por código.
